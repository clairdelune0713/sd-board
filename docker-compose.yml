services:
  sd-board:
    build: .
    ports:
      - "127.0.0.1:35014:8005"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped